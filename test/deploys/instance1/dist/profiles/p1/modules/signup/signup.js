if( typeof define != 'function' ) {
  var define = require( 'amdefine' )( module );
}

define(function( require ) {

  $ = require('jquery');
  return function() {

    // $('.email-registration-link').click(function(){
    //   $('.m-signup-form').removeClass('initial').addClass('final');
    //   $('input[name=full-name]').focus();
    // });
  }
});
