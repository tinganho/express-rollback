if( typeof define != 'function' ) {
  var define = require( 'amdefine' )( module );
}

define(function( require ) {

  $ = require( 'jquery' );

  return function( Router ) {

    // var _weiboSignup = document.querySelectorAll( '.js-signin-weibo' );

    // for( var i = 0, length = _weiboSignup.length; i < length; i++ ) {

    // 	_weiboSignup[ i ].addEventListener( 'hover', function() {

    //      Router.Signup.navigate( 'step1' );

    // 	}, true);

    // }

  }
});
